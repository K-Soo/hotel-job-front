import styled from 'styled-components';

interface RecruitmentInfoProps {}

export default function RecruitmentInfo({}: RecruitmentInfoProps) {
  return <S.RecruitmentInfo>RecruitmentInfo</S.RecruitmentInfo>;
}

const S = {
  RecruitmentInfo: styled.div``,
};
