import styled from 'styled-components';

interface ModalTemplateProps {}

export default function ModalTemplate({}: ModalTemplateProps) {
  return <S.ModalTemplate>ModalTemplate</S.ModalTemplate>;
}

const S = {
  ModalTemplate: styled.div``,
};
