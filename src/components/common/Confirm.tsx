import styled from 'styled-components';

interface ConfirmProps {}

export default function Confirm({}: ConfirmProps) {
  return <S.Confirm>Confirm</S.Confirm>;
}

const S = {
  Confirm: styled.div``,
};
