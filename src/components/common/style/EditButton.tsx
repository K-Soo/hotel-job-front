import styled from 'styled-components';

interface EditButtonProps {}

export default function EditButton({}: EditButtonProps) {
  return <S.EditButton>EditButton</S.EditButton>;
}

const S = {
  EditButton: styled.div``,
};
