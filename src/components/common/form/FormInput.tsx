import styled from "styled-components";

interface FormInputProps {}

export default function FormInput({}: FormInputProps) {
  return <S.FormInput>FormInput</S.FormInput>;
}

const S = {
  FormInput: styled.div``,
};
