import styled from "styled-components";

interface LocationProps {}

export default function Location({}: LocationProps) {
  return <S.Location>Location</S.Location>;
}

const S = {
  Location: styled.div``,
};
