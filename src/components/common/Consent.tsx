import styled from "styled-components";

interface ConsentProps {}

export default function Consent({}: ConsentProps) {
  return <S.Consent>Consent</S.Consent>;
}

const S = {
  Consent: styled.div``,
};
