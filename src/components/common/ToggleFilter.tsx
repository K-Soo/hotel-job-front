import styled from "styled-components";

interface ToggleFilterProps {}

export default function ToggleFilter({}: ToggleFilterProps) {
  return <S.ToggleFilter>ToggleFilter</S.ToggleFilter>;
}

const S = {
  ToggleFilter: styled.div``,
};
