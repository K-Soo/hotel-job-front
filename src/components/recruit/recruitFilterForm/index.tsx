import styled from 'styled-components';

interface RecruitFilterFormProps {}

export default function RecruitFilterForm({}: RecruitFilterFormProps) {
  return <S.RecruitFilterForm>RecruitFilterForm</S.RecruitFilterForm>;
}

const S = {
  RecruitFilterForm: styled.div``,
};
