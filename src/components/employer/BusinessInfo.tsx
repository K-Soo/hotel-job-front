import styled from 'styled-components';

interface BusinessInfoProps {}

export default function BusinessInfo({}: BusinessInfoProps) {
  return <S.BusinessInfo>BusinessInfo</S.BusinessInfo>;
}

const S = {
  BusinessInfo: styled.div``,
};
